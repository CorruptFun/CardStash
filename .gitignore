node_modules
dist
*.local
.DS_Store
tests/harness/fixtures/
tests/harness/report/
# Corpus sweep output: generated from bulk downloads, regenerated by
# `npm run test:corpus:full`. The 20-card fixture beside it IS committed.
tests/corpus/report/
# The bulk downloads the sweeps run against (~120 MB). Warmed on first run;
# CORPUS_CACHE points elsewhere when two agents should share one copy.
tests/corpus/.cache/

# sync server state (local only)
server/data/

# Supabase CLI scratch
supabase/.temp/
supabase/.branches/
