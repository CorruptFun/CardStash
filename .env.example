# Copy to .env.local (gitignored) for local dev, and set the same variable in the
# GitHub Actions deploy workflow for production.
#
# Google OAuth client id for free-tier backup to the user's OWN Google Drive.
# Without it, lib/drive.ts is dormant: the Backup section never renders, no
# third-party script is loaded, and the app behaves exactly as it did before the
# feature existed. Nothing else in the app reads it.
#
# Create at https://console.cloud.google.com/apis/credentials
#   · Application type: Web application
#   · Authorised JavaScript origins: your dev origin + https://corruptfun.github.io
#   · Scope requested at runtime: https://www.googleapis.com/auth/drive.appdata
#     (app-private folder only — never the user's files, which is what keeps
#     this out of Google's restricted-scope review)
#
# There is NO client secret. This is a browser token flow; the id is public and
# ships in the bundle, which is expected and safe for this OAuth model.
VITE_GOOGLE_CLIENT_ID=

# PSA public-API token, used to resolve a scanned slab's certification number
# to the exact card (lib/psa.ts). This is OUR key — there is no user-facing
# field for it — so cert lookup works with nothing to configure. Leave it empty
# and the module is dormant: no request is ever made to PSA, and slab scanning
# still works off the printed label.
#
# Create at https://www.psacard.com/publicapi (free; the token does not expire).
#
# READ THIS BEFORE SETTING IT. Unlike VITE_GOOGLE_CLIENT_ID above, this value is
# NOT safe to publish. That client id is public by design and protected by its
# origin allowlist; a PSA bearer token has no such backstop and ships readable
# inside a static bundle. Two things follow:
#
#   1. Anyone can extract it and spend it against our PSA account.
#   2. The free tier is ~100 calls a DAY for the whole account, now shared by
#      every user rather than per-person. Ordinary traffic exhausts it.
#
# Certs are cached for months and a 429 stands lookups down for hours, but the
# arithmetic is unchanged. The real fix is a proxy holding the token
# server-side with per-user rate limiting — point VITE_PSA_ENDPOINT at it and
# leave this empty.
VITE_PSA_TOKEN=

# Optional: base URL of a cert-lookup proxy, called as `<endpoint>/<cert>`.
# Overrides PSA's own endpoint so the token never has to reach the browser.
VITE_PSA_ENDPOINT=

# Buying and selling between friends (Stripe Connect escrow). OFF unless this is
# exactly `on`. Leave it empty and the marketplace is dormant: no Buy button, no
# Selling section, no orders list, and marketplace.ts refuses every call before
# it reaches the network. In-person trades are unaffected — they are local and
# serverless and share no code with any of this.
#
# THIS IS THE COSMETIC HALF. A constant in a static bundle is one devtools tab
# from being true, so the switch that actually stops a card being charged is
# `MARKETPLACE_ENABLED` on the stripe-escrow edge function (a Supabase secret,
# not a VITE_ value). Turn BOTH on, in that order — server first — or you get a
# UI that offers a purchase the server will refuse.
#
# Before turning either on: Stripe Connect must be signed up for, platform
# responsibility for losses accepted, and connected-account payouts set to
# weekly or manual. See docs/decisions.md §19.
VITE_MARKETPLACE=

# Diagnostics have NO env var and no secret. The log posts to `ingest_events()`
# on this app's own Supabase project (migration 0007), authenticated with the
# publishable key above, which already ships in the bundle. See
# src/lib/diagconfig.ts for why that is safer than a token, not laxer: the
# function is the only way in, it caps and validates every batch, and no role
# but service_role can read a row back.
#
# It is dormant wherever the vault is: a build with no Supabase project posts
# nothing. Collection is always local and always on.

# pokemontcg.io API key, ours and compiled in (lib/settings.ts). There is no
# Settings field for it — there used to be, and no real user ever filled it in.
# Optional: without one the app uses the anonymous rate limit, which is exactly
# what every user had before. Same publish caveat as VITE_PSA_TOKEN — it ships
# readable in the bundle, so treat it as identifying the app, not a secret.
VITE_POKEMON_KEY=

# --- Stripe Billing (the SUBSCRIPTION) --------------------------------------
# Set as Supabase secrets on the `stripe-billing` function, NOT as VITE_ values
# — none of these ever reach the browser:
#
#   STRIPE_SECRET_KEY               shared with stripe-escrow (same account)
#   STRIPE_PRICE_ID                 the recurring price the checkout sells
#   STRIPE_BILLING_WEBHOOK_SECRET   comma-separated, one per endpoint
#   STRIPE_BILLING_RETURN_URL       defaults to the live site
#   STRIPE_GRACE_DAYS               default 3
#
# This REPLACES square-billing. Both write `entitlements`, which is the whole
# interface — nothing in src/ knows which provider is in use. Run them in
# parallel only long enough to migrate existing subscribers, then stop
# delivering Square webhooks; two writers pushing the same expiry around is
# survivable but pointless.
#
# Stripe Billing (subscriptions) and Stripe Connect (the escrow in
# stripe-escrow) are different integrations that happen to share an account and
# a secret key. They have separate webhook endpoints and separate signing
# secrets — do NOT reuse STRIPE_WEBHOOK_SECRET here.

# The founding offer: a ONE-OFF price bought by the first 100 people who
# arrived through a friend's link. Empty = the offer is off and everyone buys
# the yearly subscription; there is no other switch.
#
# It grants `expires_at = NULL` — no expiry, ever (migration 0005). That is a
# PERMANENT liability: those users never pay again while still costing Gemini
# calls every month. Deliberate, and worth re-reading before raising 100.
#
# Seats are RESERVED at checkout and claimed on payment, so an abandoned
# checkout releases its seat rather than burning one. `seat between 1 and 100`
# plus a unique constraint is the real backstop — the arithmetic could in
# principle be raced, the constraint cannot.
STRIPE_FOUNDING_PRICE_ID=

# Free allowances — what someone with NO subscription gets each month. Set as
# Supabase secrets on the functions, not as VITE_ values.
#
#   SCAN_FREE_MONTHLY_LIMIT   default 50   (a rescue costs ~$0.00015)
#   BUILD_FREE_MONTHLY_LIMIT  default 3    (a build costs ~250x a rescue)
#
# ORDINARY SCANNING IS NOT METERED AND MUST NOT BE. It runs on the device and
# costs nothing. Only the cloud RESCUE — the fallback for a card the local
# pipeline failed to read — is counted.
